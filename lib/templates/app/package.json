{
  "name": "<%= moduleName %>",
  "description": "",
  "version": "0.0.0",
  "main": "<%= moduleFileName %>",
  "devDependencies": {
    "chai": "*",
    "mocha": "*"
  },
  "scripts": {
    "test": "mocha --no-colors --reporter spec"
  }
}
